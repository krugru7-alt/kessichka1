export const metadata = {
  title: "Для Кэссички ❤️",
  description: "for you",
};

export default function RootLayout({ children }) {
  return (
    <html lang="ru">
      <body>{children}</body>
    </html>
  );
}
